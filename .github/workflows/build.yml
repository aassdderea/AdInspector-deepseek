name: Build AdInspector Dylib

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install ldid
      run: brew install ldid

    - name: Clone Theos
      run: |
        export THEOS=/opt/theos
        sudo mkdir -p $THEOS
        sudo chown -R $USER:staff $THEOS
        git clone --recursive https://github.com/theos/theos.git $THEOS

    - name: Build dylib
      run: |
        export THEOS=/opt/theos
        export SDKROOT=$(xcrun --sdk iphoneos --show-sdk-path)
        export PREFIX=$SDKROOT/usr/
        sudo mkdir -p $SDKROOT/usr
        sudo ln -sf $(xcrun -f clang) $SDKROOT/usr/clang
        sudo ln -sf $(xcrun -f clang++) $SDKROOT/usr/clang++
        make clean
        make

    - name: Verify output
      run: |
        ls -lh .theos/obj/debug/arm64/TapSimulator.dylib || ls -lh .theos/obj/*/TapSimulator.dylib || echo "Searching..."
        find . -name "*.dylib" -type f

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: TapSimulator.dylib
        path: "**/*.dylib"
